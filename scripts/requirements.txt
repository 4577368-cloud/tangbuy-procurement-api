# 部署时子进程脚本（scripts/ 与 scripts/vendor/）所需的 Python 依赖。
# 运行环境需具备 python3；线上镜像执行： pip install -r scripts/requirements.txt
#
# - alibaba_open_cli.py（开放平台 / 牛顿云）：仅标准库，无需第三方依赖
# - newton_cli.py（B 层选品/催单）+ vendor/1688-sourcing-inquiry：requests；OAuth 配置流程用到 keyring
# - vendor/inquiry-1688（商品询盘）：requests、PyJWT
requests>=2.28.0
PyJWT>=2.0.0
keyring>=25.0
